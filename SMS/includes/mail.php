<?php	

	 function email_registration ($user_id, $email, $activation_hash){

		// if(!isset($email) || !isset($activation_hash)) {				 
			// died('We are sorry, but there appears to be a problem with the form you submitted.');       			 
		// }

		// $email_from = EMAIL_VERIFICATION_FROM;
		// $email_subject = EMAIL_VERIFICATION_SUBJECT;
		// $email_from_name = EMAIL_VERIFICATION_FROM_NAME;
		// $email_to = $email;

		// $link = EMAIL_VERIFICATION_URL.'?id='.urlencode($user_id).'&verification_code='.urlencode($activation_hash);
		// $body = EMAIL_VERIFICATION_CONTENT.' '.$link;

		// $headers = 'From: '.$email_from."\r\n".			 
		// 'Reply-To: '.$email_from."\r\n" .			 
		// 'X-Mailer: PHP/' . phpversion();

		// if (@mail($email_to, $email_subject, $body, $headers) == true)
			// return true;
		// else
			// return false;

	 }
		
?>